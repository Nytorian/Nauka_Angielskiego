<div class="section-main">
  <h1>Editing User</h1>

  <%= render 'form', user: @user %>

  <% if current_user && current_user.user_group == 0 %>
    <%= link_to 'Pokaz', @user %> |
    <%= link_to 'Wroc', users_path %>
  <% end %>
</div>
